# Example environment variables for Spring Boot datasource
# Copy this file to .env (do NOT commit the real values)
# Then set these values locally or in your deployment environment.

# MySQL / Aiven JDBC URL (including driver prefix and parameters)
# Example (local): jdbc:mysql://localhost:3306/lifewood_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
# Example (cloud): jdbc:mysql://<host>:<port>/<db_name>?ssl-mode=REQUIRED
DB_URL=

# Database username
DB_USERNAME=

# Database password (rotate if previously leaked)
DB_PASSWORD=
